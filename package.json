{
  "name": "dragndrop-poc",
  "version": "1.0.0",
  "description": "dragndrop POC - Understanding TypeScript Course Setup",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:watch": "concurrently \"tsc --watch\" \"lite-server\"",

    "build": "tsc && ./scripts/copy-static-files.sh",
    "start": "lite-server --baseDir=dist"
  },
  "keywords": [
    "typescript",
    "course"
  ],
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "lite-server": "^2.5.4"
  }
}
